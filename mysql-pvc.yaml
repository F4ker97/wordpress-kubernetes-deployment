apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  storageClassName: manual      # ¡Debe coincidir con el PV!
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi